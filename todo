 - generate files of packages to install with tags based on how to install them
 - build git install capabilities for brave-bin, st, dwm and dmenu
 - generate all the dot-files (vim, xinit, bash, bash_profile)
 - review and set key bindings (ie. alt+p for dmenu)
 - create symlink generator to put configs in correct location in the $HOME directory

 - list out all key driver installs for Mac and add logic to bootstrap: wifi - broadcom-wl, xorg/video - x86-video-intel, mesa.
